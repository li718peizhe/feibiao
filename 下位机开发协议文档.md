# 视觉-电控串口通信协议（下位机开发文档）

本文档基于最新定稿协议，用于指导电控下位机实现与视觉上位机的串口通信。

## 1. 通信参数

- 通信方式：UART
- 波特率：115200
- 数据位：8
- 停止位：1
- 校验位：无（硬件串口层）
- 流控：无
- 数据格式：二进制定长包（packed）
- 字节序：小端（Little Endian）
- 浮点格式：IEEE-754 单精度（`float`）
- 校验算法：CRC16-Modbus

---

## 2. 协议目标与原则

- 视觉端连续发送上行包，电控端自主决定是否采纳。
- 瞄准阶段主要使用 `yaw`（即 `yaw_cmd`，增量角，rad）。
- 发射阶段可按策略参考 `direction` 与 `distance_mode`。
- 视觉端接收电控回传 `yaw_current`，用于闭环计算 `yaw_cmd`。

---

## 3. 坐标与符号约定

- `yaw_current` 零位：上电后炮筒初始指向为 0。
- 符号：左正右负。
- 单位：rad。

闭环计算关系（视觉侧）：

```text
yaw_target_cam  = atan2(y_cam, x_cam)
yaw_target_base = yaw_target_cam + robot.camera_orientation.yaw
yaw_cmd         = wrapToPi(yaw_target_base - yaw_current)
```

---

## 4. 上行协议（视觉 -> 电控）

### 4.1 VisionPacket（固定 16B）

```c
#pragma pack(push, 1)
typedef struct {
    uint8_t  header;        // 0xA5
    uint8_t  state;         // 0=未发现目标, 1=发现目标
    float    yaw;           // yaw_cmd，单位rad（增量角）
    uint8_t  direction;     // 0=目标在左侧, 1=目标在右侧
    uint8_t  distance_mode; // 0=近偏差(修正2°), 1=远偏差(修正4°)
    uint32_t timestamp_ms;  // 图像捕获时间戳(ms)
    uint16_t t_offset;      // 时间补偿(ms)
    uint16_t checksum;      // CRC16-Modbus（前14字节）
} VisionPacket;
#pragma pack(pop)
```

### 4.2 字节布局

| 偏移 | 字段 | 类型 | 字节数 | 说明 |
|---|---|---|---:|---|
| 0 | `header` | `uint8_t` | 1 | 固定 `0xA5` |
| 1 | `state` | `uint8_t` | 1 | 0 无目标 / 1 有目标 |
| 2 | `yaw` | `float` | 4 | 增量角指令（rad） |
| 6 | `direction` | `uint8_t` | 1 | 0 左 / 1 右 |
| 7 | `distance_mode` | `uint8_t` | 1 | 0 近偏差 / 1 远偏差 |
| 8 | `timestamp_ms` | `uint32_t` | 4 | 采集时间戳（ms） |
| 12 | `t_offset` | `uint16_t` | 2 | 时间补偿（ms） |
| 14 | `checksum` | `uint16_t` | 2 | CRC16，小端 |

### 4.3 语义约束

- 无目标时：
  - `state=0`
  - `yaw=0`
  - `direction=0`
  - `distance_mode=0`
- `distance_mode` 是弹着偏差档位，不是绝对距离档位。

---

## 5. 下行协议（电控 -> 视觉）

### 5.1 ControlFeedbackPacket（固定 12B）

```c
#pragma pack(push, 1)
typedef struct {
    uint8_t  header;       // 0x5A
    float    yaw_current;  // 当前炮筒yaw，rad（上电零位，左正右负）
    uint8_t  accept_yaw;   // 0=当前不采纳视觉yaw, 1=采纳视觉yaw
    uint32_t timestamp_ms; // 电控时间戳(ms)
    uint16_t checksum;     // CRC16-Modbus（前10字节）
} ControlFeedbackPacket;
#pragma pack(pop)
```

### 5.2 字节布局

| 偏移 | 字段 | 类型 | 字节数 | 说明 |
|---|---|---|---:|---|
| 0 | `header` | `uint8_t` | 1 | 固定 `0x5A` |
| 1 | `yaw_current` | `float` | 4 | 当前炮筒 yaw（rad） |
| 5 | `accept_yaw` | `uint8_t` | 1 | 0 不采纳 / 1 采纳 |
| 6 | `timestamp_ms` | `uint32_t` | 4 | 电控时间戳（ms） |
| 10 | `checksum` | `uint16_t` | 2 | CRC16，小端 |

---

## 6. CRC 与编码规范

CRC16-Modbus 统一规范：

- 初始值：`0xFFFF`
- 多项式：`0xA001`
- 输出：16 位，低字节在前（小端）
- 计算范围：
  - 上行 `VisionPacket`：前 14 字节（偏移 `0..13`）
  - 下行 `ControlFeedbackPacket`：前 10 字节（偏移 `0..9`）

参考实现：

```c
#include <stdint.h>
#include <stddef.h>

uint16_t crc16_modbus(const uint8_t* data, size_t len) {
    uint16_t crc = 0xFFFF;
    for (size_t i = 0; i < len; i++) {
        crc ^= data[i];
        for (int j = 0; j < 8; j++) {
            if (crc & 0x0001) {
                crc = (crc >> 1) ^ 0xA001;
            } else {
                crc >>= 1;
            }
        }
    }
    return crc;
}
```

---

## 7. 下位机接收上行包（VisionPacket）示例

```c
#include <stdint.h>
#include <stddef.h>
#include <string.h>

#pragma pack(push, 1)
typedef struct {
    uint8_t  header;
    uint8_t  state;
    float    yaw;
    uint8_t  direction;
    uint8_t  distance_mode;
    uint32_t timestamp_ms;
    uint16_t t_offset;
    uint16_t checksum;
} VisionPacket;
#pragma pack(pop)

int parse_vision_packet(const uint8_t* buf, size_t len, VisionPacket* out) {
    const size_t PKT_LEN = 16;
    if (len < PKT_LEN) return -1;
    if (buf[0] != 0xA5) return -2;

    memcpy(out, buf, PKT_LEN);
    uint16_t crc = crc16_modbus(buf, 14);
    if (crc != out->checksum) return -3;
    return 0;
}
```

---

## 8. 下位机发送下行包（ControlFeedbackPacket）示例

```c
#include <stdint.h>
#include <string.h>

#pragma pack(push, 1)
typedef struct {
    uint8_t  header;
    float    yaw_current;
    uint8_t  accept_yaw;
    uint32_t timestamp_ms;
    uint16_t checksum;
} ControlFeedbackPacket;
#pragma pack(pop)

void build_feedback_packet(ControlFeedbackPacket* pkt,
                           float yaw_current,
                           uint8_t accept_yaw,
                           uint32_t timestamp_ms) {
    pkt->header = 0x5A;
    pkt->yaw_current = yaw_current;
    pkt->accept_yaw = accept_yaw ? 1 : 0;
    pkt->timestamp_ms = timestamp_ms;
    pkt->checksum = 0;
    pkt->checksum = crc16_modbus((const uint8_t*)pkt, 10);
}
```

---

## 9. 联调检查清单

1. `sizeof(VisionPacket) == 16`
2. `sizeof(ControlFeedbackPacket) == 12`
3. 双方都使用 packed（或手动按字节序列化）
4. 双方 CRC 算法与计算范围一致（14B / 10B）
5. `yaw_current` 零位与符号一致（上电零位，左正右负）
6. 无目标时上行字段按约定置零
7. 长时间运行下，连续解析无丢帧/无卡死

---

## 10. 常见问题

**Q1：CRC 总失败？**  
A：先检查 CRC 范围是否正确：上行 14 字节、下行 10 字节；其次检查结构体是否 packed、小端是否一致。

**Q2：`yaw` 符号看起来反了？**  
A：确认双方都使用“左正右负”，且 `yaw_current` 以“上电初始指向=0”为零位。

**Q3：为什么 `direction` 和 `yaw` 看似重复？**  
A：`direction` 主要给发射阶段策略使用，`yaw` 主要用于瞄准闭环，两者保留以便下位机按阶段选取字段。

**Q4：`distance_mode` 还没做完怎么办？**  
A：可先固定发送 `0`，不影响主链路通信与闭环调试。

